# Requirements

## Operating system

The artifact has been tested on macOS.

## Python

Python 3.11 or higher is required.

## Python dependencies

All required packages are listed in `03-scripts/requirements.txt`.
Install them inside a virtual environment (see the README for setup instructions).

## Storage

The `02-train-data/` directory will be populated when training scripts are run.
